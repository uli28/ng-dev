- HTML events can be converted to observable streams using the `fromEvent` operator. 

  ```typescript
  fromEvent(this.canvas.nativeElement, 'mousemove')
  ```

- Click "Subscribe Mouse Events" and hover over the canvas

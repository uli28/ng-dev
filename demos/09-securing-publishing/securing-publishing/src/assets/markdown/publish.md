- Install firebase tools:

    ```bash
    npm install -g firebase-tools
    ```

- Login:

    ```bash
    firebase login
    ```

- Init Project & Deploy:

    ```bash
    firebase init
    firebase deploy
    ```
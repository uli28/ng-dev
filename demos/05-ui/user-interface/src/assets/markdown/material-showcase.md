- Angular Material is a UI component library for Angular that provides a set of reusable UI components based on Google’s Material Design. 

- To add material to an app, execute:

  ```bash
  ng add @angular/material
  ```

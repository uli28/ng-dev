- Attribute binding allows us to bind to html attributes and is using the square brackets syntax `[]`

``` html
  <input type="text"
    placeholder="Enter name"
    [disabled]="isDisabled"
    [hidden]="isHidden"
  />
  ```
